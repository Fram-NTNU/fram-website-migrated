import { handleFramkompass } from "@/lib/framkompass";

export const runtime = "nodejs";

export const POST = handleFramkompass;
export const GET = handleFramkompass;
export const PUT = handleFramkompass;
export const PATCH = handleFramkompass;
export const DELETE = handleFramkompass;
export const OPTIONS = handleFramkompass;
